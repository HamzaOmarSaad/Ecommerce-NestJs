export enum orderStatusEnum {
  PENDING,
  PLACED,
  ON_WAY,
  DELIVERED,
  CANCELED,
  REFUNDED,
}
export enum PaymentTypeEnum {
  CASH,
  VISA,
}
export enum currencyEnum {
  EGP = 'egp',
  USD = 'usd',
}
